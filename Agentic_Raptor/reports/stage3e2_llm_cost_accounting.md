# Stage 3E.2 LLM-DPO — cost accounting

Date: 2026-07-26

LLM campaign SPICE calls: 0 new (all evidence reused from Stage 3E.2 artifacts — no double counting; dedup keys). Wall clock: sft+dpo recorded per-parts CPU. SFT 29.8s; DPO 49.4s.
