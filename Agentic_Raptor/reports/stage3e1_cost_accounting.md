# Stage 3E.1 — cost accounting

Date: 2026-07-26

Per-episode exact accounting (ep0): expansions=6, validator=6, value_net=7, mbsac_leaf=1, real_spice=2, cache_hits=0, sim_failures=0, leaf_budget_exhausted=0, wall=5.2s. Cache hits never count as SPICE calls (keyed by role-aware graph hash). Limits: max_simulations, max_expansions, max_real_spice_calls, max_depth, max_children all enforced.
