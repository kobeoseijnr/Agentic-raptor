# Stage 3E.3 — model selection

Date: 2026-07-26

distilgpt2 @ main, Apache-2.0, 81.9M params, ctx 1024, no quantisation, CPU fp32 train+infer (no CUDA — GPU deferred). Provider-independent interface; fixture provider preserved for tests. Text+structured-graph context only — NOT claimed multimodal-trained.
